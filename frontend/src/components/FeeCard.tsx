import type {Calculation} from '../types';
const money=(n:number)=>new Intl.NumberFormat('en-IN',{style:'currency',currency:'INR',maximumFractionDigits:2}).format(n);
export function FeeCard({calc}:{calc:Calculation|null}){if(!calc)return <div className="fee muted">Complete the form to see a live fee preview.</div>;return <div className="fee"><h3>Fee preview</h3><div className="breakdown"><span>Area</span><b>{calc.areaInSqm} sqm</b><span>Restoration</span><b>{money(calc.restorationCharge)}</b><span>Permission</span><b>{money(calc.permissionFee)}</b><span>Urgency surcharge</span><b>{money(calc.urgencySurcharge)}</b><span>Security deposit</span><b>{money(calc.securityDeposit)}</b><strong>Total</strong><strong>{money(calc.totalAmount)}</strong></div><small>reviewRef: {calc.reviewRef}</small></div>}
